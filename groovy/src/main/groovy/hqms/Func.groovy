package hqms

//Closure substring = { String val1, int val2, int val3 -> val1.substring(val2, val3) }
//Closure contains = { String val1, val2 -> val1.contains(val2) }
//Closure ascii = { String val -> val.length() > 1 ? false : (int) val }
//
//boolean exec(GString arg) {
//    println(arg)
////    return {->arg}
//}

// .substring(0,1)).indexOf("李")
